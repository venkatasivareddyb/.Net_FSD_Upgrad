@{
    ViewData["Title"] = "Student List";
    var students = (List<string>)ViewData["Students"];
}
<h1>Student List</h1>

<h2>@ViewData["Title"]</h2>

<ul>
    @foreach (var student in students)
    {
        <li>@student</li>
    }
</ul>

